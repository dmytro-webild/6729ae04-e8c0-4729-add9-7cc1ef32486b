import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { DM_Sans } from "next/font/google";



export const metadata: Metadata = {
  title: 'Sai Vivek Wood Exports | Premium Indian Sandalwood & Agarwood',
  description: 'Leading global exporters of aged Indian Red Sandalwood, White Sandalwood, and Agarwood for luxury perfume and cosmetic manufacturers.',
  openGraph: {
    "title": "Sai Vivek Wood Exports | Global Premium Wood Sourcing",
    "description": "Authentic, high-grade sandalwood and agarwood exports for discerning B2B global manufacturers.",
    "type": "website",
    "siteName": "Sai Vivek Wood Exports"
  },
};

const dmSans = DM_Sans({
  variable: "--font-dm-sans",
  subsets: ["latin"],
});
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${dmSans.variable} ${inter.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
